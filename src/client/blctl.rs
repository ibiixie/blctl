// use backlight::{Backlight, Sysfs};

struct Blctl {
    // backlight: Box<dyn Backlight>
}

impl Blctl {
    fn new() -> Self {
        Self {
            // backlight: Box::new(Sysfs::new());
        }
    }
}